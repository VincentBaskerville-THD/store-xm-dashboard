
  # Store XM Dashboard

  This is a code bundle for Store XM Dashboard. The original project is available at https://www.figma.com/design/7aCXf9XY7sYFYgFS0wYn9W/Store-XM-Dashboard.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  